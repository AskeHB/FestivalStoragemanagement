<UserControl x:Class="CarnivalSoragemanagement.UI.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarnivalSoragemanagement.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Label Content="Himmerland Festival" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBox Text="{Binding SelectedUser.StudentIdCard}"  Grid.Row="1" Height="20" Width="200" VerticalAlignment="Bottom"/>
        <Button Content="Login" Grid.Row="2" Command="{Binding LoginButton}" Width="100" Height="20" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
